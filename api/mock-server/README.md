# Mock-server
## How-to
`cd api/mock-sever && npm install && npm start`

## Routes supported
Currently, we are supporting these routes in our mock-server:
GET `/planning`
GET `/modules`
GET `/events`
GET `/activities`
